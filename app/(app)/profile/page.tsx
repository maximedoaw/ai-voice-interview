// /profile
// 👉 Affiche et permet la modification du profil utilisateur
// 👉 Données depuis useAuth() → UserProfile (types/user.ts)

export default function ProfilePage() {
  return (
    <div>
      {/* Profil utilisateur — à implémenter */}
    </div>
  )
}
